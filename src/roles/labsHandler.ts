export class LabsHandler
{
    static run(creep: Creep, labs: StructureLab[], boostAmount: number)
    {
      // First 2 labs are ingredient labs. All the others can be used for cooking boosts
      // Transporter can have different tasks : 'filling', 'collecting'

    }
}
